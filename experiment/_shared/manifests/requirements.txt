numpy
pyyaml
tqdm
torch>=1.10

# Optional dependencies (install when needed):
# transformers  # for local HF LLM backend
# requests      # for legacy HTTP LLM backend

# Gate B formal live prior via OFOX OpenAI-compatible API:
# openai  # install with: python -m pip install -U openai

# Historical B2-live Gemini adapter only; no longer the formal provider:
# google-genai
